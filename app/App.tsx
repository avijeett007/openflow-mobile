/**
 * Entry component. Registered by `index.js` via `registerRootComponent`.
 * The full app (providers, navigation, deep-link hop handler) lives in `src/`.
 */
export { default } from '../src/App';
